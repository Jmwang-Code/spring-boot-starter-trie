server:
  port: 8081
  address: 127.0.0.1
spring:
  application:
    name: JMW.Tire
  profiles:
    active: dev

# springdoc-openapi
springdoc:
  swagger-ui:
    path: /swagger-ui.html
#    tags-sorter: alpha
#    operations-sorter: alpha
  #api-docs:
    #path: /v3/api-docs
#  group-configs:
#    - group: 'jmw'
#      paths-to-match: '/**'
#      packages-to-scan: com.xiaominfo.knife4j.demo.web
# knife4j
#knife4j:
#  enable: true
#  setting:
#    language: zh_cn

#logging:
#  config: classpath:logback.xml
#  file:
#    path: log
#  pattern:
#    file: '%d %5p - %m%n'

provider:
  name: myProvider
  source: mySource
  dataSources:
    - type: myType1
      driverClassName: myDriver1
      url: myUrl1
      username: myUsername1
      password: myPassword1
      sql: select * from myTable
    - type: myType2
      driverClassName: myDriver2
      url: myUrl2
      username: myUsername2
      password: myPassword2
      sql: select * from myTable